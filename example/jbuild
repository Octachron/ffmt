(jbuild_version 1)

(executable (
            (name interpolation)
            (preprocess (pps (ppxi)))
            (libraries (metafmt))
))

(alias
 ((name    runtest)
  (deps    (interpolation.exe))
  (action  (run ${<}))
))
