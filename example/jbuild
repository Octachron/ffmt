(jbuild_version 1)

(executable (
            (name example)
            (preprocess (pps (ppx)))
            (libraries (metafmt))
))

(alias
 ((name    runtest)
  (deps    (example.exe))
  (action  (run ${<}))
))
