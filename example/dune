
(tests
  (names interpolation)
  (modules Interpolation)
  (preprocess (pps ffmt_ppxi))
  (libraries ffmt)
)

(tests
  (names meta)
  (modules Meta)
  (preprocess (pps ffmt_ppx))
  (libraries ffmt)
)
